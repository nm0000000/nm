# Алгоритм сортировки выбором (Selection Sort)
**Алгоритм сортировки выбором (Selection Sort)** — это простой метод сортировки массива элементов путем последовательного выбора минимального элемента и перемещения его в начало отсортированной части массива.

### Объяснение алгоритма:
Ищем минимальный элемент массива с помощью for (int i = 0; i < size - 1; i++). Меняем его местами с первым элементом с помощью функции swap(). Затем повторяем процесс для оставшейся части массива (исключив первый элемент) с помощью функции for (int j = i + 1; j < size; j++). Повторяем шаги пока весь массив не станет отсортированным.

### Временная сложность: O(n²)

### Объяснение временной сложности:
Наличие двух циклов, внешнего и внутреннего, каждый из которых зависит от n, вызывает временную сложность O(n²).

### Пример работы алгоритма:
Исходный массив: 64 25 12 22 11 

Отсортированный массив: 11 12 22 25 64 


# Алгоритм сортировки обменом (или метод пузырьковой сортировки)
**Алгоритм сортировки обменом (или метод пузырьковой сортировки)** — это один из наиболее простых способов сортировки элементов массива путем многократного сравнения соседних элементов и их обмена местами при нарушении порядка.

### Объяснение алгоритма:
Основная функция bubbleSort реализует сам алгоритм пузырьковой сортировки. Вспомогательная функция printArray отображает содержимое массива перед и после сортировки. Наружный цикл (for i...) регулирует общее количество проходов по массиву. Поскольку после каждого прохода больший элемент гарантированно сдвигается ближе к концу, последующие проходы обходят всё меньше элементов массива. Во внутреннем цикле (for j...) происходят непосредственные сравнение и обмен элементов с помощью функции swap(). Дополнительная переменная swapped помогает сократить лишние вычисления.

### Временная сложность: O(n²)

### Объяснение временной сложности:
Наличие двух циклов, внешего и внутреннего, каждый из которых зависит от n, вызывает временную сложность O(n²).

### Пример работы алгоритма:
Исходный массив: 64 34 25 12 22 11 90 

Отсортированный массив: 11 12 22 25 34 64 90 


# Алгоритм сортировки вставками (Insertion Sort)
**Алгоритм сортировки вставками (Insertion Sort)** - алгоритм упорядочивания элементов массива, идея которого заключается в постепенном построении отсортированного участка массива путём пошагового добавления новых элементов в нужное положение внутри уже отсортированной части.

### Объяснение алгоритма:
Сначала создаётся массив целых чисел. Потом запускается функция insertionSort, которая осуществляет саму сортировку вставками. Внутри неё используются два цикла: внешний цикл for (int i = 1; i < size; ++i) перебирает элементы массива, а внутренний цикл while (j >= 0 && arr[j] > key) находит правильное место для очередного элемента среди уже отсортированных. По завершении сортировки выводится отсортированный массив с помощью функции printArray.

### Временная сложность: O(n²)

### Объяснение временной сложности:
Наличие двух циклов, внешего и внутреннего, каждый из которых зависит от n, вызывает временную сложность O(n²).

### Пример работы алгоритма:
Исходный массив: 8 3 5 1 9

Отсортированный массив: 1 3 5 8 9 


# Алгоритм сортировки слиянием (Merge Sort)
**Алгоритм сортировки слиянием (Merge Sort)** - это эффективный алгоритм сортировки, основанный на методе "разделяй и властвуй", основная идея которого заключается в рекурсивном разделении массива на две половины, каждая из которых сортируется отдельно, а затем обе части объединяются обратно в единый отсортированный массив путём сравнения элементов обеих частей.

### Объяснение алгоритма:
Определяем базовую операцию разделения массива на две равные части с помощью вычисления середины массива. Используя функцию mergeSort(), рекурсивно применяем сортировку к левой половине массива, затем к правой половине. Когда оба подмассива отсортированы, используем вспомогательную функцию merge(), которая объединяет два отсортированных подмассива в один общий отсортированный массив с помощью циклов while. Это объединение выполняется путём попарного сравнения элементов слева и справа c помощью if и вставки меньших значений в итоговый массив. Выводим массив на экран с помощью функции печати массива printArray().

### Временная сложность: O(nlogn)

### Объяснение временной сложности:
На каждом уровне рекурсии массив делится на две равные части, а потом эти части сливаются обратно. Поскольку глубина дерева рекурсии равна log2n уровней, а на каждом уровне выполняются операции объединения размером O(n), общая временная сложность составляет O(nlogn).

### Пример работы алгоритма:
Исходный массив: 12 11 13 5 6

Отсортированный массив: 5 6 11 12 13


# Алгоритм сортировки Шелла (Shell Sort)
**Алгоритм сортировки Шелла (Shell Sort)** — это улучшенная версия алгоритма вставок. Основная идея заключается в том, чтобы сначала упорядочить элементы, находящиеся далеко друг от друга, постепенно уменьшая расстояние между элементами до тех пор, пока оно не достигнет единицы (стандартная сортировка вставками).

### Объяснение алгоритма:
Начинаем с некоторого большого значения шага (gap), которое меньше размера массива. Для каждого такого шага выполняем сортировку вставками среди элементов, находящихся на расстоянии gap, с помощью цикла for. После каждой итерации значение шага уменьшается с помощью цикла for (int gap = n / 2; gap > 0; gap /= 2), и снова выполняется сортировка вставками. Когда шаг становится равным единице, алгоритм фактически превращается в обычную сортировку вставками, но теперь массив почти отсортирован, что значительно ускоряет этот последний этап. Выводим массив с помощью функции printArray().

### Временная сложность: O(n^1.25)

### Объяснение временной сложности:
После каждого прохода шаг уменьшается вдвое, начиная с половины размера массива. Сначала выполняются крупные перемещения, которые эффективно перераспределяют элементы, сокращая общую работу на последующих итерациях. Засчёт этого удается достичь среднего показателя временной сложности O(n^1.25)

### Пример работы алгоритма:
Исходный массив: 12 34 54 2 3 

Отсортированный массив: 2 3 12 34 54

# Алгоритм быстрой сортировки (Quicksort)
**Алгоритм быстрой сортировки (Quicksort)** — это эффективный способ сортировки массивов и списков по принципу "разделяй и властвуй", что означает разделение проблемы на более мелкие подзадачи, решение каждой из них отдельно, а затем объединение результатов.

### Объяснение алгоритма:
Выбираем последний элемент массива в качестве опорного (pivot). Используя процедуру partition, с помощью цикла for (int j = low; j < high; j++) перемещаем все элементы, меньшие или равные опорному, слева от него, а большие элементы справа. Ставим опорный элемент на свое место в отсортированном массиве с помощью swap(). Рекурсивно (quickSort) применяем тот же алгоритм к двум оставшимся частям массива (до и после опорного элемента).

### Временная сложность: 𝑂(𝑛\log𝑛)

### Объяснение временной сложности:
Каждый раз глубина рекурсии уменьшает длину обрабатываемого массива примерно вдвое, что даёт логарифмическое число рекурсивных вызовов (\log𝑛), а на каждой глубине приходится просматривать весь массив (𝑛). Общая сложность: 𝑂(𝑛\log𝑛).

### Пример работы алгоритма:
Исходный массив: 10 7 8 9 1 5

Отсортированный массив: 1 5 7 8 9 10


# Алгоритм пирамидальной сортировки (HeapSort)
**Алгоритм пирамидальной сортировки (HeapSort)** - алгоритм сортировки, суть метода которого заключается в построении бинарной кучи и последовательном извлечении элементов после, перестраивая кучу каждый раз заново, пока массив не окажется отсортированным.

### Объяснение алгоритма:
Для каждого внутреннего узла проверяется условие, что родительская вершина больше обеих дочерних вершин. Если это не так, то родитель меняется с большим ребёнком с помощью swap и рекурсивно применяется процедуру heapify для восстановленного поддерева. Строится максимальная куча с помощью цикла for (int i = n / 2 - 1; i >= 0; i--). Начиная с середины массива (так как листья сами по себе являются кучами) осуществляется проход назад к началу, применяя heapify ко всей структуре. Сортировка: каждый раз берётся верхний элемент (корень), перемещается в конец массива с помощью swap в цикле for, уменьшая длину кучи, и снова heapify, сохраняя структуру. Функция print_array выводит отсортированный массив.

### Временная сложность: O(nlogn)

### Объяснение временной сложности:
Каждый проход (извлечение вершины и перестроение) занимает O(log n) и выполняется n раз, что вызывает общую временную сложность O(nlogn).

### Пример работы алгоритма:
Исходный массив: 12 11 13 5 6 7

Отсортированный массив: 5 6 7 11 12 13

